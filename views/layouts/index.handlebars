<h1>Burgers</h1>

<h2>Eat Da Burgers</h2>

<ul>
  {{#each burger}}
    {{#unless sleepy}}
      {{> burger/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Type of burger:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Enter a burger you want to eat. Click the submit button</button>
</form>
